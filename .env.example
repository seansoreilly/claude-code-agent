# === Config (stays in .env on server) ===

# Gateway server port
PORT=8080

# Claude Agent SDK settings
CLAUDE_MODEL=claude-sonnet-4-6
CLAUDE_MAX_TURNS=25
CLAUDE_MAX_BUDGET_USD=5

# Working directory for Claude operations
CLAUDE_WORK_DIR=/home/ubuntu/workspace

# Memory storage directory
MEMORY_DIR=/home/ubuntu/.claude-agent/memory

# === Secrets (managed via Bitwarden — see scripts/sync-secrets.sh) ===
# These are injected by sync-secrets.sh from the Bitwarden vault folder
# "claude-agent-lightsail". Do NOT manually edit these on the server.
#
# TELEGRAM_BOT_TOKEN=...        # Telegram bot API token from @BotFather
# TELEGRAM_ALLOWED_USERS=...    # Comma-separated allowed Telegram user IDs
# GH_TOKEN=...                  # GitHub personal access token
# ICAL_URL=...                  # Google Calendar iCal feed URL
# GOOGLE_MAPS_API_KEY=...       # Google Maps API key
# FACEBOOK_APP_ID=...           # Facebook app ID
# FACEBOOK_APP_SECRET=...       # Facebook app secret
# FACEBOOK_PAGE_ID=...          # Facebook page ID
# FACEBOOK_PAGE_TOKEN=...       # Facebook page access token
# ANTHROPIC_API_KEY=...         # Anthropic API key (for email triage)
